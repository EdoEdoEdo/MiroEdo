fastapi>=0.115,<1.0
uvicorn[standard]>=0.32
pydantic>=2.9,<3.0
eval_type_backport>=0.2     # Pydantic 2 + Python 3.9 union syntax (`int | None`)
python-multipart>=0.0.12
pandas>=2.2
httpx>=0.27
openai>=1.54                # Engine modules (profile_generator, report_agent, ...)
python-dotenv>=1.0
pytest>=8.2

# ============= File parsing =============
PyMuPDF>=1.24.0             # PDF text extraction (fitz)
openpyxl>=3.1.0             # XLSX reader for pandas.read_excel
charset-normalizer>=3.0.0   # Encoding detection for non-UTF8 text files
chardet>=5.0.0              # Encoding detection fallback

# NOTE: OASIS (camel-oasis, camel-ai) and Zep are in requirements-simulation.txt.
# They require Python >=3.10,<3.12 and are installed only in the Docker image
# (python:3.11-slim). Local dev on Python 3.9 runs everything except simulation.

