[project]
name = "miroedo-backend"
version = "0.1.0"
description = "MiroEdo backend — Brandwatch ingestion + MiroFish postprocess"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
